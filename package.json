{
  "name": "code",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "http-server . -p 8080 -c-1",
    "build": "echo 'No build process needed for static files'",
    "start": "npm run dev"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}